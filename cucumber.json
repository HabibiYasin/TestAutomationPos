{
  "default": {
    "paths": ["tests/features/**/*.feature"],
    "requireModule": ["ts-node/register"],
    "require": ["tests/steps/**/*.ts", "tests/support/**/*.ts"],
    "format": ["progress", "allure-cucumberjs/reporter", "html:reports/report.html"]
  }
}
